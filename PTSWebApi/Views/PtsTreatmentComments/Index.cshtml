@model IEnumerable<PTSWebApi.Models.PtsTreatmentComment>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Treatment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Comment.CommentText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Treatment.TreatmentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TreatmentCommentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TreatmentCommentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TreatmentCommentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
