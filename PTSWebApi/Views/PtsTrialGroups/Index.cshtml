@model IEnumerable<PTSWebApi.Models.PtsTrialGroup>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TrialGroupNr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FieldBlock)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plant)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TrialGroupNr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FieldBlock.BlockChar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plant.PlantName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrialGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrialGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrialGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
