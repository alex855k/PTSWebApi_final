@model IEnumerable<PTSWebApi.Models.PtsTrialBlock>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TrialBlockDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrialEnd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FieldBlock)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrialType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TrialBlockDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrialEnd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FieldBlock.BlockChar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrialType.TrialTypeId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrialBlockId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrialBlockId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrialBlockId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
