@model IEnumerable<PTSWebApi.Models.PtsTreatmentType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TreatmentTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TreatmentTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TreatmentTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TreatmentTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TreatmentTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
