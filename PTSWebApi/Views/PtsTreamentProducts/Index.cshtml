@model IEnumerable<PTSWebApi.Models.PtsTreamentProduct>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductDose)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Treatment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductDose)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.ProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Treatment.TreatmentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TreamentProductId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TreamentProductId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TreamentProductId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
