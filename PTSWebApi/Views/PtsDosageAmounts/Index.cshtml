@model IEnumerable<PTSWebApi.Models.PtsDosageAmount>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ResultValue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DosageType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnitType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResultValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DosageType.DosageTypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnitType.UnitTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DosageAmountId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DosageAmountId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DosageAmountId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
